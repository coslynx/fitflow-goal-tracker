import mongoose from 'mongoose';
import dotenv from 'dotenv';

dotenv.config();

/**
 * Establishes a connection to the MongoDB database.
 *
 * @returns {Promise<void>} A Promise that resolves when the database connection is successful, or rejects if there is an error.
 * @throws {Error} If the MONGODB_URI environment variable is not set or if there is an issue connecting to the database.
 *
 * @example
 *